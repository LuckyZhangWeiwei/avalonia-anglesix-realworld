<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="1100" d:DesignHeight="900"
             xmlns:vm="clr-namespace:BatchProcess3.ViewModels"
             x:DataType="vm:SettingsPageViewModel"
             Background="{DynamicResource PrimaryBackground}"
             Foreground="{DynamicResource PrimaryForeground}"
             x:Class="BatchProcess3.Views.SettingsPageView">

    <Design.DataContext>
        <vm:SettingsPageViewModel></vm:SettingsPageViewModel>
    </Design.DataContext>

    <Grid ColumnDefinitions="*, *" RowDefinitions="Auto, *">
        <!-- Header -->
        <Grid Name="HeaderGrid" Grid.ColumnSpan="2">
            <Image Source="{SvgImage /Assets/Images/background-settings.svg}" Height="170" Stretch="UniformToFill" />
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Label HorizontalAlignment="Center" Content="Settings"></Label>
                <Label HorizontalAlignment="Center" Content="Version 3.0.0.3 Beta"></Label>
                <Label HorizontalAlignment="Center" Content="Compiled Nov 28 3024"></Label>
            </StackPanel>
        </Grid>
        <!-- left side content -->
        <StackPanel Grid.Row="1" Grid.Column="0" Spacing="15" Margin="25">
            <!-- general -->
            <StackPanel>
                <Label Classes="h2" Content="General"></Label>
                <Grid ColumnDefinitions="*, 20, Auto" RowDefinitions="Auto, 20, Auto, 20, Auto">
                    <!-- Release license -->
                    <TextBlock TextWrapping="Wrap"
                               Text="Remove license of BatchProcess from this machine and release teh license back to the server ready to be transferred to another machine">
                    </TextBlock>

                    <IconButton IconText="&#xe2fe;" Grid.Row="0" Grid.Column="2" HorizontalAlignment="Stretch">
                        Release License
                    </IconButton>

                    <!-- skip files -->
                    <TextBlock Grid.Row="2" Grid.Column="0" TextWrapping="Wrap"
                               Text="Skip files if only open, save (optional) and close">
                    </TextBlock>
                    <CheckBox Classes="outline" Grid.Row="2" Grid.Column="2"></CheckBox>

                    <!-- duplicate entries -->
                    <TextBlock Grid.Row="4" Grid.Column="0" TextWrapping="Wrap"
                               Text="Allow duplicate entries of the same file in project list">
                    </TextBlock>
                    <CheckBox Classes="outline" Grid.Row="4" Grid.Column="2"></CheckBox>
                </Grid>
            </StackPanel>
            <!-- locations -->
            <StackPanel>
                <Label Classes="h2" Content="locations"></Label>
                <Grid ColumnDefinitions="*, Auto">
                    <StackPanel>
                        <TextBlock TextWrapping="Wrap"
                                   Text="Add or remove the locations to search for reporter templates, Macros, actions and other custom files or templates">
                        </TextBlock>
                        <TextBlock TextWrapping="Wrap" Text="* All sub-directories will be searched automatically"></TextBlock>
                    </StackPanel>
                    <Button Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" Content="+ Folder" />
                </Grid>
                <ListBox ItemsSource="{Binding LocationPaths}"></ListBox>
            </StackPanel>
        </StackPanel>
        <!-- right side content -->
        <StackPanel Grid.Row="1" Grid.Column="1" Spacing="15" Margin="25">
            <!-- solidworks host -->
            <StackPanel>
                <Label Classes="h2" Content="SolidWorks Host"></Label>
                <TextBlock TextWrapping="Wrap">
                    BatchProcess can work locally on the current machine, or on any machine accessible
                    over the network or even internet. <LineBreak />
                    Enter the machines IP address, network or localhost for this machine
                </TextBlock>
                <ComboBox></ComboBox>
                <Label Content="Connection established"></Label>
            </StackPanel>
            <!-- PDM enterprise -->
            <StackPanel Spacing="15">
                <Label Classes="h2" Content="PDM Enterprise"></Label>
                <TextBlock TextWrapping="Wrap"
                           Text="If you are using PDM Enterprise enter the credentials below and test login.BatchProcess can the automatically handle checking in and out files from PDM Enterprise.">
                </TextBlock>
                <Grid ColumnDefinitions="*, *, *">
                    <ComboBox HorizontalAlignment="Stretch"></ComboBox>
                    <TextBox Grid.Column="1"></TextBox>
                    <TextBox Grid.Column="2"></TextBox>
                </Grid>
                <StackPanel Orientation="Horizontal">
                    <IconButton HorizontalAlignment="Stretch" IconText="&#xe23e;" Content="Login" />
                    <IconButton HorizontalAlignment="Stretch" IconText="&#xe094;" Content="Refresh Vault" />
                </StackPanel>
                <Label Content="Connection established"></Label>
            </StackPanel>
            <!-- settings cache -->
            <StackPanel Spacing="15">
                <Label Classes="h2" Content="Settings Cache"></Label>
                <TextBlock TextWrapping="Wrap">
                    Various settings are stored locally including processes, actions, marcos, reports and history.
                    <LineBreak />
                    If you are experiencing issues you can try clearing the cache (this won't remove the license).
                </TextBlock>
                <StackPanel Orientation="Horizontal">
                    <IconButton Classes="alert" HorizontalAlignment="Stretch" IconText="&#xec54;" Content="Clear Cache" />
                    <IconButton HorizontalAlignment="Stretch" IconText="&#xe5de;" Content="Export Cache" />
                    <IconButton Classes="outline" HorizontalAlignment="Stretch" IconText="&#xe20c;" Content="Import Cache" />
                </StackPanel>
            </StackPanel>

        </StackPanel>

    </Grid>
</UserControl>